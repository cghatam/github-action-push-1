name: Push Trigger demo

on:
  push:
    branches:
      - main

jobs:
  demo-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Show message
        run : echo "Workflow statred"

      - name: List files
        run: ls -ltr

      - name: Read file
        run: cat app.txt
